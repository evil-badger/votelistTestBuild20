const SmilesBestResult = (props) => {
    console.log();
    return (
        <div className="SmilesBestItem">
            <p></p>
            <p>{props.bestResult}</p>

        </div>
    )
}

export default SmilesBestResult